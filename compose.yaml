services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - "${APP_PORT:-8000}:8000"
        volumes:
            - app-data:/var/www/html/database
        restart: unless-stopped

volumes:
    app-data:
